---
description: "Согласование кода и документации при расхождениях"
globs:
  - "docs/**/*"
  - "src/**/*"
---

# Принципы
- Истина — код. Если дока устарела, следуй коду, но зафиксируй несоответствие.
- Укажи конкретные файлы/строки, где поведение кода отличается.

# Что писать в ответе
- Коротко: что расходится (конкретные поля/сигнатуры).
- Как правильно согласно коду (укажи файл, экспорт).
- Что нужно обновить в доке (карточка/refs/спецификация).

# Пример формулировки
> Документация `docs/src/content/docs/10-modules/example.md#Outputs` утверждает, что поле `role` строка. В коде (`src/domain/user/getUser.ts`, метод `mapRole`) возвращается `roleId: number`. Следую коду. Требуется обновление docs.

# Sources
- Приводи **оба** источника: файл доки и файл кода.